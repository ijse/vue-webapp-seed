machine:
  node:
    version: v6.6.0

dependencies:
  override:
    - npm install --dev

  post:
    - wget https://selenium-release.storage.googleapis.com/3.0-beta4/selenium-server-standalone-3.0.0-beta4.jar
    - java -jar selenium-server-standalone-3.0.0-beta4.jar:
        background: true

